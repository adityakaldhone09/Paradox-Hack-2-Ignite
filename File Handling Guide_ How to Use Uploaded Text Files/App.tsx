import { useState } from 'react';
import { motion } from 'framer-motion';
import {
  ArrowRight, BadgeCheck, Bell, Blocks, Check, ChevronDown, CircleAlert, ClipboardCheck,
  Clock3, Eye, FileCheck2, Fingerprint, Hash, Layers3, LockKeyhole, Menu, Network,
  PanelLeft, Plus, Radar, ScanSearch, ShieldCheck, Sparkles, TimerReset, Users, X,
} from 'lucide-react';
import { roleConfig, roleOptions, type VeriQRole } from './config/roles';

const features = [
  { icon: Fingerprint, title: 'Cryptographic integrity', text: 'Every paper is hashed, signed, and verifiable from creation to access.' },
  { icon: LockKeyhole, title: 'Secure encryption', text: 'Sensitive content stays protected at rest and in transit.' },
  { icon: TimerReset, title: 'Time-locked access', text: 'Release windows make the right content available at the right moment.' },
  { icon: Users, title: 'Centre authorization', text: 'Role-based controls keep distribution precise and accountable.' },
  { icon: Blocks, title: 'Blockchain audit', text: 'A tamper-evident record captures every meaningful action.' },
  { icon: Radar, title: 'Security monitoring', text: 'Surface anomalies early with a clear, calm operational view.' },
];

const custody = [
  ['Created', 'A. Rao', '08:12:04'], ['Approved', 'S. Mehta', '08:16:28'], ['Encrypted', 'VeriQ system', '08:16:31'],
  ['Assigned', 'Centre C102', '08:17:10'], ['Released', 'Time window', '09:00:00'], ['Accessed', 'Invigilator', '09:02:18'], ['Verified', 'SHA-256', '09:02:19'],
];

const navItems = ['Overview', 'Examinations', 'Papers', 'Centres', 'Devices', 'Security', 'Incidents', 'Blockchain', 'Audit'];

function Logo({ light = false }: { light?: boolean }) {
  return <div className={`logo ${light ? 'logo-light' : ''}`}><span className="logo-mark"><img src="/assets/veriq-logo-mark.svg" alt="" /></span><span>VeriQ</span></div>;
}

function Button({ children, onClick, secondary = false, small = false }: { children: React.ReactNode; onClick?: () => void; secondary?: boolean; small?: boolean }) {
  return <button className={`button ${secondary ? 'button-secondary' : ''} ${small ? 'button-small' : ''}`} onClick={onClick}>{children}</button>;
}

function HeroVisual() {
  const steps = ['Question paper', 'Encrypted', 'SHA-256 verified', 'Authorized', 'Released', 'Blockchain recorded'];
  return <div className="hero-visual">
    <div className="visual-topline"><span className="eyebrow">LIVE PROTECTION FLOW</span><span className="live-dot" /> <span className="muted">All systems operational</span></div>
    <div className="flow-list">{steps.map((step, i) => <motion.div key={step} className={`flow-row ${i === 0 ? 'flow-first' : ''}`} initial={{ opacity: 0, x: 15 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: i * .13, duration: .35 }}><span className={`flow-icon ${i === steps.length - 1 ? 'flow-final' : ''}`}>{i === 0 ? <FileCheck2 size={17} /> : <Check size={17} />}</span><span>{step}</span>{i > 1 && <span className="flow-chip">verified</span>}</motion.div>)}</div>
    <div className="visual-footer"><span><span className="pulse" /> Encrypted session</span><code>QPR-2026-0917</code></div>
  </div>;
}

function Landing({ onSignIn }: { onSignIn: () => void }) {
  const [story, setStory] = useState(2);
  const custodySteps = [
    ['Create', 'Structured drafting with schema validation', FileCheck2],
    ['Encrypt', 'AES-256-GCM authenticated payload sealing', LockKeyhole],
    ['Hash', 'SHA-256 mathematical digest generation', Hash],
    ['Authorize', 'Centre, device, and superintendent verification', Users],
    ['Release', 'Time-locked synchronized delivery window', TimerReset],
    ['Verify', 'Dual-hash tamper comparison', ShieldCheck],
    ['Audit', 'Immutable ledger trail for review', Blocks],
  ];
  return <div className="landing-page">
    <header className="site-nav"><Logo /><div className="nav-context"><span className="nav-pulse" />Secure control plane</div><nav>{['Product', 'Security', 'Architecture', 'Features'].map(x => <a href={`#${x.toLowerCase()}`} key={x}>{x}</a>)}</nav><div className="nav-actions"><button className="text-button" onClick={onSignIn}>Sign in</button><Button small onClick={onSignIn}>Get started <ArrowRight size={15} /></Button></div><button className="mobile-menu"><Menu size={20} /></button></header>
    <main>
      <section className="hero section-grid"><div className="hero-copy"><div className="kicker"><span className="kicker-line" /> Examination security, made visible</div><h1>Secure every question paper. <em>Verify every action.</em></h1><p className="hero-sub">VeriQ protects examination papers with cryptographic security, controlled access, and blockchain-backed chain of custody.</p><div className="hero-actions"><Button onClick={onSignIn}>Explore VeriQ <ArrowRight size={16} /></Button><Button secondary onClick={onSignIn}>Sign in</Button></div><div className="hero-proof"><BadgeCheck size={16} /> Designed for institutional-grade examination workflows</div></div><HeroVisual /></section>
      <section className="trust-strip"><span className="muted">BUILT FOR HIGH-TRUST WORKFLOWS</span>{['Encrypted', 'Tamper-evident', 'Time-controlled', 'Role-based', 'Auditable'].map((x, i) => <span className="trust-item" key={x}>{[LockKeyhole, ShieldCheck, Clock3, Users, ClipboardCheck][i]({ size: 15 })}{x}</span>)}</section>
      <section className="section section-narrow" id="features"><div className="section-heading"><div><span className="eyebrow">ONE SYSTEM, CLEAR CONTROL</span><h2>Security that works quietly.</h2></div><p>From authoring to audit, VeriQ gives every examination event a clear and verifiable place in the chain.</p></div><div className="feature-grid">{features.map(({ icon: Icon, title, text }) => <motion.div className="feature" key={title} whileHover={{ y: -3 }}><div className="feature-icon"><Icon size={19} /></div><h3>{title}</h3><p>{text}</p></motion.div>)}</div></section>
      <section className="story section" id="product">
        <div className="section-heading"><div><span className="eyebrow">THE SECURE PAPER LIFECYCLE</span><h2>Make every handoff accountable.</h2></div><p>One connected workflow for the people, papers, and places that make an examination possible.</p></div>
        <div className="story-layout">
          <div className="story-steps">{['Create', 'Protect', 'Authorize', 'Release', 'Verify', 'Audit'].map((x, i) => <button className={`story-step ${story === i ? 'active' : ''}`} onClick={() => setStory(i)} key={x}><span>0{i + 1}</span><b>{x}</b><ArrowRight size={15} /></button>)}</div>
          <motion.div className="story-card" layout>
            <div className="story-card-head"><span className="status-pill"><span className="status-dot green" /> {['Draft created', 'Encryption complete', 'Centre approved', 'Window active', 'Integrity verified', 'Audit sealed'][story]}</span><code>QPR-2026-0917</code></div>
            <div className="story-art"><div className="paper-sheet"><div className="paper-lock"><LockKeyhole size={22} /></div><span>QUESTION PAPER</span><strong>Computer Science</strong><small>Semester examination · 2026</small><div className="paper-lines"><i /><i /><i /></div></div><div className="orbit orbit-a" /><div className="orbit orbit-b" /><div className="story-callout"><Hash size={14} /><span>Hash registered</span><code>A89D21...F8A1</code></div></div>
            <div className="story-card-foot"><span>{['A paper begins with a controlled creation event.', 'Content is encrypted before it leaves the authoring workspace.', 'Only authorised centres can receive the release.', 'Access opens only inside the defined time window.', 'A live comparison confirms the document is unchanged.', 'The complete trail is sealed for independent audit.'][story]}</span><span className="step-count">0{story + 1} / 06</span></div>
          </motion.div>
        </div>
      </section>
      <section className="section security-section" id="security"><div className="section-heading centered"><span className="eyebrow">DEFENCE IN DEPTH</span><h2>Trust, built into the architecture.</h2><p>VeriQ separates the controls that protect content from the controls that authorize people.</p></div><div className="architecture"><div className="arch-node root"><ShieldCheck size={18} />VeriQ control plane</div><div className="arch-lines"><i /><i /><i /></div><div className="arch-branches"><div><LockKeyhole /><b>Encryption</b><small>Protect the content</small></div><div><Users /><b>Authorization</b><small>Control the access</small></div><div><Fingerprint /><b>Integrity</b><small>Prove the state</small></div></div><div className="arch-down" /><div className="arch-node audit"><Blocks size={18} />Blockchain audit record</div></div></section>
      <section className="section section-narrow" id="architecture"><div className="section-heading"><div><span className="eyebrow">CHAIN OF CUSTODY</span><h2>A timeline you can trust.</h2></div><p>Every significant event is time-stamped, attributed, and ready to verify.</p></div><div className="custody">{custody.map(([event, actor, time], i) => <div className="custody-row" key={event}><div className="custody-line"><span className={i === custody.length - 1 ? 'last' : ''}>{i === custody.length - 1 ? <Check size={13} /> : <span />}</span></div><div className="custody-event"><b>{event}</b><span>{actor}</span></div><time>{time}</time><span className="status-pill tiny"><span className="status-dot green" />Recorded</span></div>)}</div></section>
      <section className="tamper section"><div className="tamper-copy"><span className="eyebrow">INTEGRITY AT A GLANCE</span><h2>If a paper changes, VeriQ knows.</h2><p>Compare the document fingerprint against its registered record in seconds—not after the exam is over.</p><Button onClick={onSignIn}>See verification flow <ArrowRight size={16} /></Button></div><div className="compare"><div className="hash-card"><span className="hash-label"><Check size={14} /> Original record</span><code>A89D21 7B4C 92EF F8A1</code><span className="hash-status good">MATCH</span></div><div className="compare-arrow"><span>document modified</span><ArrowRight size={18} /></div><div className="hash-card altered"><span className="hash-label"><CircleAlert size={14} /> Received copy</span><code>D71F40 3C2B 11AE 9A31</code><span className="hash-status bad">MISMATCH</span></div></div></section>
      <section className="section roles-section"><div className="section-heading centered"><span className="eyebrow">ONE PLATFORM, FOUR CLEAR ROLES</span><h2>Everyone knows what comes next.</h2></div><div className="role-grid">{roleOptions.map(([role, cfg], i) => <div className="role-card" key={role}><span className="role-number">0{i + 1}</span><h3>{cfg.label}</h3><p>{cfg.description}</p><ArrowRight size={16} /></div>)}</div></section>
      <section className="cta section"><span className="eyebrow">READY WHEN YOU ARE</span><h2>Secure your examination workflow.</h2><p>Bring every paper, centre, and action into one verifiable system.</p><Button onClick={onSignIn}>Enter VeriQ <ArrowRight size={16} /></Button></section>
    </main><footer className="site-footer"><div className="footer-brand"><Logo /><p>Secure every question paper.<br />Verify every action.</p></div><div className="footer-col"><b>Platform</b><a href="#product">How it works</a><a href="#security">Security architecture</a><a href="#features">Features</a></div><div className="footer-col"><b>Trust</b><a href="#architecture">Chain of custody</a><a href="#security">Integrity controls</a><a href="#product">Audit trail</a></div><div className="footer-meta"><span><span className="footer-status" /> Systems operational</span><small>© 2026 VeriQ · Institutional security platform</small></div></footer>
  </div>;
}

function Auth({ onComplete }: { onComplete: (role: VeriQRole) => void }) {
  const [role, setRole] = useState<VeriQRole>('SUPER_ADMIN'); const [showPassword, setShowPassword] = useState(false); const [email, setEmail] = useState('');
  return <div className="auth-page"><div className="auth-aside"><Logo light /><div><span className="eyebrow">SECURE EXAMINATION CONTROL</span><h1>Confidence in every handoff.</h1><p>VeriQ gives institutions a precise, tamper-evident record of how examination papers move.</p></div><div className="auth-aside-foot"><ShieldCheck size={16} /> Protected workspace</div></div><div className="auth-main"><div className="auth-panel"><div className="auth-mobile-logo"><Logo /></div><span className="eyebrow">WELCOME BACK</span><h2>Sign in to VeriQ</h2><p className="muted">Secure access to your VeriQ workspace.</p><label>Email<input value={email} onChange={e => setEmail(e.target.value)} placeholder="you@institution.edu" type="email" /></label><label>Password<div className="password-input"><input placeholder="Enter your password" type={showPassword ? 'text' : 'password'} /><button onClick={() => setShowPassword(!showPassword)}>{showPassword ? <Eye size={17} /> : <Eye size={17} />}</button></div></label><div className="auth-row"><label className="check-label"><input type="checkbox" /> Remember me</label><button className="link-button">Forgot password?</button></div><Button onClick={() => onComplete(role)}>Sign in <ArrowRight size={16} /></Button><div className="auth-divider"><span>Demo role</span></div><div className="role-selector">{roleOptions.map(([key, cfg]) => <button className={role === key ? 'selected' : ''} onClick={() => setRole(key)} key={key}><span className="role-selector-icon">{key === 'SUPER_ADMIN' ? <ShieldCheck size={15} /> : key === 'PAPER_SETTER' ? <FileCheck2 size={15} /> : key === 'CENTRE_ADMIN' ? <Network size={15} /> : <ScanSearch size={15} />}</span>{cfg.label}</button>)}</div><div className="protected-note"><LockKeyhole size={14} /> Your connection is encrypted and protected.</div></div></div></div>;
}

function Dashboard({ role, onExit }: { role: VeriQRole; onExit: () => void }) {
  const [active, setActive] = useState('Overview'); const [verifiedPaper, setVerifiedPaper] = useState(false); const cfg = roleConfig[role];
  return <div className="app-shell"><aside className="sidebar"><div className="sidebar-top"><Logo /><button className="collapse"><PanelLeft size={16} /></button></div><div className="workspace-switch"><span className="workspace-mark">V</span><span><b>VeriQ workspace</b><small>National examination board</small></span><ChevronDown size={15} /></div><nav className="side-nav"><span className="side-label">WORKSPACE</span>{navItems.map(item => <button className={active === item ? 'active' : ''} onClick={() => setActive(item)} key={item}>{item === 'Overview' ? <Layers3 size={16} /> : item === 'Papers' ? <FileCheck2 size={16} /> : item === 'Security' ? <ShieldCheck size={16} /> : item === 'Blockchain' ? <Blocks size={16} /> : item === 'Centres' ? <Network size={16} /> : <ClipboardCheck size={16} />}{item}{item === 'Incidents' && <span className="nav-count">2</span>}</button>)}</nav><div className="sidebar-bottom"><button onClick={() => setActive('Settings')}><PanelLeft size={16} />Settings</button><div className="persona-switch"><span className="persona-label">PERSONA</span>{roleOptions.map(([key, item]) => <button className={role === key ? 'persona-option selected' : 'persona-option'} key={key} onClick={() => key !== role && onExit()}><span className="persona-dot" />{item.label}{role === key && <Check size={13} />}</button>)}</div><button className="user-mini" onClick={onExit}><span className="avatar">AR</span><span><b>Alex Rao</b><small>{cfg.label}</small></span><ChevronDown size={14} /></button></div></aside><main className="dashboard-main"><header className="dash-topbar"><div className="breadcrumbs"><span>Workspace</span><span>/</span><b>{active}</b></div><div className="dash-actions"><button className="icon-button"><Bell size={17} /><i /></button><div className="avatar">AR</div></div></header><div className="dash-content"><div className="dash-heading"><div><span className="eyebrow">THURSDAY, 17 SEPTEMBER 2026</span><h1>Good morning, Alex.</h1><p>Here’s the security picture for your examination workspace.</p></div>{role !== 'INVIGILATOR' && <Button small><Plus size={16} /> New examination</Button>}{role === 'INVIGILATOR' && <div className="role-context"><ScanSearch size={15} /> Invigilator workspace · verification ready</div>}</div><div className="metric-row">{[['Secured papers', '128', '+12 this cycle', FileCheck2], ['Active examinations', '08', '03 releasing today', ClipboardCheck], ['Verified centres', '42', '100% ready', Network], ['Security events', '02', 'Both resolved', ShieldCheck]].map(([label, value, trend, Icon]) => <div className="metric" key={label as string}><div className="metric-top"><span>{label as string}</span><Icon size={17} /></div><strong>{value as string}</strong><small><span className={(label as string) === 'Security events' ? 'orange' : ''}>↗ {trend as string}</span></small></div>)}</div><div className="workspace-grid"><section className="panel activity-panel"><div className="panel-header"><div><h2>Examination activity</h2><p>Paper lifecycle events across the workspace</p></div><button className="select-button">Last 30 days <ChevronDown size={14} /></button></div><div className="chart"><div className="chart-y"><span>40</span><span>30</span><span>20</span><span>10</span><span>0</span></div><div className="chart-body"><div className="chart-lines"><i /><i /><i /><i /><i /></div><svg viewBox="0 0 620 180" preserveAspectRatio="none"><path d="M0 145 C45 130 58 142 94 112 S145 125 178 88 S230 98 268 108 S310 63 350 75 S400 55 430 69 S475 40 510 58 S560 34 620 16" fill="none" stroke="#2563eb" strokeWidth="3" /><path d="M0 145 C45 130 58 142 94 112 S145 125 178 88 S230 98 268 108 S310 63 350 75 S400 55 430 69 S475 40 510 58 S560 34 620 16 V180 H0Z" fill="url(#area)" opacity=".22" /><defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop stopColor="#2563eb" /><stop offset="1" stopColor="#fff" stopOpacity="0" /></linearGradient></defs></svg><div className="chart-x"><span>18 Aug</span><span>25 Aug</span><span>01 Sep</span><span>08 Sep</span><span>17 Sep</span></div></div></div></section><section className="panel events-panel"><div className="panel-header"><div><h2>Recent security events</h2><p>Latest activity requiring attention</p></div><button className="more-button">•••</button></div><div className="event-list">{[['Paper integrity verified', 'PAP-CS-03 · 2 min ago', 'good', Check], ['Release window opened', 'EXM-2026-09 · 18 min ago', 'blue', TimerReset], ['Early access attempt blocked', 'Centre C102 · 42 min ago', 'orange', CircleAlert], ['Centre device authorised', 'CEN-042 · 1 hr ago', 'good', BadgeCheck]].map(([title, sub, tone, Icon]) => <div className="event-row" key={title as string}><span className={`event-icon ${tone as string}`}><Icon size={15} /></span><span><b>{title as string}</b><small>{sub as string}</small></span><ArrowRight size={15} /></div>)}</div><button className="panel-link" onClick={() => setActive('Security')}>View all security activity <ArrowRight size={14} /></button></section></div><section className="panel papers-panel"><div className="panel-header"><div><h2>Latest secured papers</h2><p>Recent papers entering the chain of custody</p></div><div className="paper-actions">{role === 'CENTRE_ADMIN' && <button className="verify-paper-button" onClick={() => setVerifiedPaper(true)}>{verifiedPaper ? <><Check size={13} /> Paper verified</> : <><ScanSearch size={13} /> Verify assigned paper</>}</button>}<button className="panel-link" onClick={() => setActive('Papers')}>View all papers <ArrowRight size={14} /></button></div></div><table><thead><tr><th>Paper</th><th>Examination</th><th>Version</th><th>Status</th><th>Integrity</th><th>Updated</th></tr></thead><tbody>{[['PAP-CS-03', 'Computer Science · Sem 1', 'v2.1', 'Released', 'Verified', '2 min ago'], ['PAP-MA-07', 'Applied Mathematics · Sem 1', 'v1.4', 'Time-locked', 'Verified', '18 min ago'], ['PAP-PH-02', 'Physics · Sem 1', 'v3.0', 'Assigned', 'Verified', '1 hr ago'], ['PAP-DB-11', 'Database Systems · Sem 1', 'v1.2', 'Approved', 'Verified', '3 hrs ago']].map(row => <tr key={row[0]}>{row.map((cell, i) => <td key={cell}><span className={i === 0 ? 'mono strong' : i === 3 ? `status-pill tiny ${cell === 'Released' ? 'released' : cell === 'Time-locked' ? 'locked' : ''}` : i === 4 ? 'integrity' : ''}>{i === 4 && <Check size={12} />}{cell}</span></td>)}</tr>)}</tbody></table></section></div></main></div>;
}

export default function App() {
  const [view, setView] = useState<'landing' | 'auth' | 'dashboard'>('landing'); const [role, setRole] = useState<VeriQRole>('SUPER_ADMIN');
  if (view === 'auth') return <Auth onComplete={(selected) => { setRole(selected); setView('dashboard'); }} />;
  if (view === 'dashboard') return <Dashboard role={role} onExit={() => setView('landing')} />;
  return <Landing onSignIn={() => setView('auth')} />;
}
